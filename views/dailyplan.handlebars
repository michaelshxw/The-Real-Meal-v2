<div class="row">
  <div class="col" id="rightScreen">
    <h3>Daily Meal Plan</h3>
    <div class="row">
      <div class="col-lg-4">
        <div class="card text-center dailyPlanCards">
          <div class="card-header">
            Recipe One
          </div>
          <div class="card-body">
            <h5 class="card-title recipeTitle">{{recipe-title.[0]}}</h5>
            <p class="card-text dailyPlanDescription">{{recipe-description.[0]}}</p>
            <a href="#" class="btn btn-primary">Show Me The Recipe</a>
          </div>
        </div>
      </div>
      <div class="col-lg-4">
        <div class="card text-center dailyPlanCards">
          <div class="card-header">
            Recipe One
          </div>
          <div class="card-body">
            <h5 class="card-title recipeTitle">{{recipe-title.[1]}}</h5>
            <p class="card-text dailyPlanDescription">{{recipe-description.[1]}}</p>
            <a href="#" class="btn btn-primary">Show Me The Recipe</a>
          </div>
        </div>
      </div>
      <div class="col-lg-4">
        <div class="card text-center dailyPlanCards">
          <div class="card-header">
            Recipe One
          </div>
          <div class="card-body">
            <h5 class="card-title recipeTitle">{{recipe-.[2]}}</h5>
            <p class="card-text dailyPlanDescription">{{recipe-description.[2]}}</p>
            <a href="#" class="btn btn-primary">Show Me The Recipe</a>
          </div>
        </div>
      </div>
    </div>

  </div>
</div>

<script src="./js/dailyplan.js"></script>

{{!-- REFERENCES --}}

{{!-- Add event listeners to dynamically created elements --}}
{{!-- https://davidwalsh.name/event-delegate --}}